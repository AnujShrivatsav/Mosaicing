cmake_minimum_required(VERSION 3.0)
project(ImageMosaicing)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS}, inc)
add_executable(ImageMosaicing src/main.cpp)
target_link_libraries(ImageMosaicing ${OpenCV_LIBS})